		Explanation for application

	The code for application (solution of assigment 2) is written in IDE Eclipse. It can be started from Eclipse or similar IDE. 
Main class is in AppTest.java .The application can be started from main class, right click > run > run as java application. Console window shows results 
of the application task. Data can be changed in main class, for checking other results of output correctivity.
 
	
